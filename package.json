{
  "name": "agenticpm",
  "module": "server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "tui": "bun tui-project.ts",
    "tui:demo": "bun tui-project.ts --demo",
    "tui:legacy": "bun tui.ts",
    "tui:project": "bun tui-project.ts",
    "tui:project:demo": "bun tui-project.ts --demo",
    "typecheck": "bunx tsc --noEmit",
    "test": "playwright test",
    "test:install": "bunx playwright install chromium"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.142",
    "hono": "^4.12.18",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
